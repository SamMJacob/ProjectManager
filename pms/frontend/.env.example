# Set these in Vercel project environment variables

# Full URL to your Railway backend API (no trailing slash)
VITE_API_URL=https://your-app.railway.app/api

# Hostname only (no protocol) for WebSocket connections
VITE_BACKEND_HOST=your-app.railway.app
